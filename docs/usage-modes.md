# Usage Modes

LensMosaic now uses a single-origin architecture.

Example:

```text
https://lens-mosaic-demo-xxxxx.run.app/
```

The UI uses the same origin for:

- static assets
- search endpoints
- item detail lookups
- live WebSocket endpoints

For local development, start the hosted app locally and open that same local URL.
